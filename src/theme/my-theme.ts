import { DefaultTheme } from 'styled-components';

const myTheme: DefaultTheme = {
  colors: {
    black: '#000000',
    white: '#FFFFFF',
    mainColor: 'cyan',
    secondaryColor: 'magenta',
  },
};

export { myTheme };
